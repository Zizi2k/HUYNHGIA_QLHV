# Dev & production default: /api (Vite proxy locally, Vercel rewrite on deploy)
# Override only if API is on another domain without proxy:
# VITE_API_URL=https://example.com/api
VITE_JITSI_DOMAIN=meet.jit.si
